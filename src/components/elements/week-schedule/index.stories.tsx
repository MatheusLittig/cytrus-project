import { Meta } from '@storybook/react'
import { WeekSchedule } from './index'

export default {
  title: 'Week schedule'
} as Meta

export const WeekScheduleStory = (): JSX.Element => <WeekSchedule />
